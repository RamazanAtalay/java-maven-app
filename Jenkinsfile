pipeline {
    agent none
    tools {
        maven 'Maven'
    }
    stages {
        stage('test') {
            steps {
                script {
                    echo "Building the application..."
                    echo "Executing pipeline for the branch"
                   // sh 'mvn package'
                }
                }
            }
        }
        stage('build') {
           steps {
                script {
                    echo "Building the application..."
                    echo "Executing pipeline for the branch"
                   // sh 'mvn package'
                }
            }
        }
        stage('deploy') {
              steps {
                script {
                    echo "Building the application..."
                    echo "Executing pipeline for the branch"
                   // sh 'mvn package'
                }
            }
        }
        
    }
